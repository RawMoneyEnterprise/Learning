/**
 * DB query client — re-exported from @/lib/neon for backwards compatibility.
 *
 * Import from @/lib/neon directly for new code:
 *   import { db } from "@/lib/neon";
 */
export { db } from "@/lib/neon";
